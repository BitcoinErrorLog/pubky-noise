# uniffi.toml
# Configuration for UniFFI binding generation

[bindings.kotlin]
# Note: UniFFI generates package "uniffi.pubky_noise" by default.
# Bitkit-android uses "com.pubky.noise" - this requires manually 
# changing the package declaration after copying the bindings.
# See docs/DOWNSTREAM_INTEGRATION_REPORT.md for details.

[bindings.swift]
module_name = "PubkyNoise"
