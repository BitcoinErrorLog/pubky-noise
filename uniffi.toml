# uniffi.toml
# Configuration for UniFFI binding generation

[bindings.kotlin]
# Generate with package name "com.pubky.noise" for Bitkit compatibility
package_name = "com.pubky.noise"

[bindings.swift]
module_name = "PubkyNoise"
