# src/ffi

UniFFI boundary for `pubky-noise`.

This directory contains the exported types and functions used by Swift and Kotlin consumers. Public changes here are API changes for mobile bindings and should be reflected in:

- `docs/FFI_GUIDE.md`
- `CHANGELOG.md`
- any generated binding outputs (`generated-swift/`, `generated-kotlin/`, and platform packaging)


