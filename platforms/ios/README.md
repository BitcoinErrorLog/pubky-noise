# platforms/ios

iOS packaging for `pubky-noise`.

- Swift Package metadata in `Package.swift`.
- Example app code in `platforms/ios/example/`.
- Swift/UniFFI artifacts used by the Swift Package live under `platforms/ios/Sources/`.

Version should match `Cargo.toml` and `CHANGELOG.md`.


