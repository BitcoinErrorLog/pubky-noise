# platforms/android

Android packaging for `pubky-noise`.

- Provides an Android library module (Gradle/Maven publishing metadata).
- Includes a small example app in `platforms/android/example/`.
- Java/Kotlin sources under `platforms/android/src/` are primarily UniFFI-generated bindings.

Version should match `Cargo.toml` and `CHANGELOG.md`.


